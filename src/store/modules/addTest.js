/* eslint no-shadow: ["error", { "allow": ["state"] }], no-param-reassign: "error" */

// import db from '../../config';

// inital state
const state = {
  isLoading: false,
};

// mutations
const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
};
